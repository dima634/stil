## C++ and Rust
C++ is used to build shared plugin library which is not supported by `cxxqt` yet, see https://github.com/KDAB/cxx-qt/issues/1266

## Installing QML plugin
```bash
cmake --install /home/dima/repos/stil/out/build/(debug|release)
```
